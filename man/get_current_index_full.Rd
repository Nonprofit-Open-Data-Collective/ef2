% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_utils_get_gt_index.R
\name{get_current_index_full}
\alias{get_current_index_full}
\title{Load the full IRS 990 e-filer index from the Data Commons}
\usage{
get_current_index_full(TIMEOUT = 600)
}
\arguments{
\item{TIMEOUT}{Integer. Download timeout in seconds (default 600).}
}
\value{
A \code{data.table} with one row per filing. See \link{index} for the column
definitions.
}
\description{
Downloads the most recent "all years" IRS 990 e-file index
published to the Giving Tuesday Data Commons and returns it as a
\code{data.table}. This is the complete index of every available filing.
}
\details{
Locates the newest \code{index_all_years_...} CSV by walking backward
from today (see \code{\link[=find_current_index_full]{find_current_index_full()}}), downloads it, sorts by
\code{ReturnTs}, and drops duplicate \code{URL}s (keeping the most recent). A count
of filings per \code{TaxYear} is printed as a side effect.
}
\examples{
\dontrun{
df <- get_current_index_full()
}
}
\seealso{
\code{\link[=get_current_index_batch]{get_current_index_batch()}} for the incremental "latest only"
index; \code{\link[=find_current_index_full]{find_current_index_full()}} to resolve just the URL.
}
