% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_utils_get_gt_index.R
\name{get_current_index_batch}
\alias{get_current_index_batch}
\title{Load the most recent IRS 990 e-filer batch index from the Data Commons}
\usage{
get_current_index_batch(TIMEOUT = 600)
}
\arguments{
\item{TIMEOUT}{Integer. Download timeout in seconds (default 600).}
}
\value{
A \code{data.table} with one row per filing. See \link{index} for the column
definitions.
}
\description{
Downloads the most recent "latest only" IRS 990 e-file index
published to the Giving Tuesday Data Commons and returns it as a
\code{data.table}. Unlike \code{\link[=get_current_index_full]{get_current_index_full()}}, this contains only the
filings that are new to the most recent batch.
}
\details{
Locates the newest \code{index_latest_only_...} CSV by walking backward
from today (see \code{\link[=find_current_index_batch]{find_current_index_batch()}}), downloads it, sorts by
\code{ReturnTs}, and drops duplicate \code{URL}s (keeping the most recent). A count
of filings per \code{TaxYear} is printed as a side effect.
}
\examples{
\dontrun{
df <- get_current_index_batch()
}
}
\seealso{
\code{\link[=get_current_index_full]{get_current_index_full()}} for the complete all-years index;
\code{\link[=find_current_index_batch]{find_current_index_batch()}} to resolve just the URL.
}
