% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05_build_database.R
\name{build_database}
\alias{build_database}
\title{Build a DuckDB database from batches of XML filings (parallel safe)}
\usage{
build_database(
  year,
  urls = NULL,
  group.size = 25,
  ccf = NULL,
  path = ".",
  is_update = FALSE,
  workers = NULL
)
}
\arguments{
\item{year}{Integer. Year label for the database.}

\item{urls}{Optional character vector of XML URLs to process. If NULL,
resumes from existing batch files in the year's folder.}

\item{group.size}{Integer batch size (default = 25).}

\item{ccf}{Concordance crosswalk object (optional).}

\item{path}{Directory in which to store year subfolder and database files.}

\item{is_update}{Logical; if TRUE, this is an incremental update build rather
than a full-year rebuild.}

\item{workers}{Optional integer number of parallel worker processes. If NULL
(default) uses the conservative \code{min(4, availableCores()/2)}. Because the
work is network-bound (each worker mostly waits on XML downloads), setting
this above the physical core count can substantially raise throughput for
large jobs. Be considerate of the source S3 endpoint when raising it.}
}
\value{
Invisibly returns the path to the merged DuckDB database.
}
\description{
Processes XML batches in parallel, with each worker writing to its own DuckDB
shard. When all workers complete, the shards are merged into a unified
database for the specified year.
}
\examples{
\dontrun{
build_database(2021, urls = urls, group.size = 25, path = "data")
build_database(2024, urls = urls, path = "data", workers = 10)
}
}
