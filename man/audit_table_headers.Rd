% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_audit_table_headers.R
\name{audit_table_headers}
\alias{audit_table_headers}
\title{Audit TABLE.HEADERS for cross-table misfires}
\usage{
audit_table_headers(
  TABLE.HEADERS = get_table_headers(),
  cc = NULL,
  verbose = TRUE
)
}
\arguments{
\item{TABLE.HEADERS}{Named list of header xpaths per table. Defaults to
\code{get_table_headers()}.}

\item{cc}{Concordance data frame with at least \code{xpath} and \code{rdb_table}.
Defaults to the packaged \code{concordance}.}

\item{verbose}{Print a summary. Default \code{TRUE}.}
}
\value{
A data frame, one row per (table, foreign table) misfire, with
columns \code{table_name}, \code{steals_from}, \code{n_xpaths}, \code{example_xpath}. Zero rows
means no header can capture another table's xpaths. Carries attribute
\code{"unmatched"}: xpaths the concordance assigns to a table whose header does
NOT match them (the opposite failure -- silently missing columns).
}
\description{
\code{build_rdb_table()} selects a table's rows with an \strong{unanchored} regular
expression:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{hd <- gsub( "//", "/", TABLE.HEADERS[[ table_name ]] )
xpath_versions <- paste0( hd, collapse = "|" )
dplyr::filter( grepl( xpath_versions, XPATH2 ) )
}\if{html}{\out{</div>}}

Because \code{grepl()} matches anywhere in the string, a header that is a
substring of another header captures rows that belong to a different table.
The IRS part naming makes this easy to hit: \code{Form990ScheduleRPartI} is a
substring of \code{Form990ScheduleRPartII}, \code{...PartIII} and \code{...PartIV}, so a
pre-2013 \code{SR-P01} extraction silently absorbs Parts II, III and IV.

This is decidable without touching any data: run every header regex against
every xpath in the concordance, and compare the \code{rdb_table} the concordance
assigns to each matched xpath against the table being built.

The audit reports both directions. Measured against the packaged concordance:
31 collisions across 17 tables (603 xpaths captured by the wrong table), and
12 xpaths in 2 tables that their own header does NOT match, so those columns
never appear in the output at all.

Note the header list is structurally complete -- 62 one-to-many tables, 62
entries -- so no collision here is caused by a MISSING table entry. They are
caused by entries that are present but are prefixes of one another.

SCOPE. \code{TABLE.HEADERS} has exactly one consumer, \code{build_rdb_table()}. The
\code{TABLE_HEADER} column in \code{FLATXML} is computed per row by \code{get_header()} from
the xpath itself and does not read this list. So if \code{build_rdb_table()} is
ever switched to select on \code{RDB_TABLE} (see \code{dev/UPSTREAM-ISSUES.md}, EF2-6
Step 1), the list stops deciding anything and this function becomes obsolete
rather than merely needing new expectations. Do not treat it as passing
evidence about a pipeline that no longer consults headers.
}
