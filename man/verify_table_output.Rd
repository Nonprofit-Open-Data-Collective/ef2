% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_write_table_output.R
\name{verify_table_output}
\alias{verify_table_output}
\title{Verify that two serialisations of a table hold identical data}
\usage{
verify_table_output(csv_path, parquet_path, con, fold_empty = TRUE)
}
\arguments{
\item{csv_path}{Path or URL to the CSV.}

\item{parquet_path}{Path or URL to the Parquet file.}

\item{con}{DBI connection to DuckDB.}

\item{fold_empty}{Logical; treat \code{''} and NULL as equal on both sides.}
}
\value{
A one-row data frame: row counts, column counts, digests, and \code{ok}.
}
\description{
Compares a CSV and a Parquet file cell for cell without assuming either has a
unique key. Both are read all-VARCHAR, every row is hashed, and the hashes are
aggregated in sorted order, so the digest is independent of row order and of
ties.
}
\details{
Ordering by \code{OBJECTID} is not sufficient for a whole-table digest. In
\code{F9-P07-T02-CONTRACTORS-2023} there are 299,205 rows but only 215,278
distinct \code{OBJECTID} values, so any \verb{ORDER BY OBJECTID} aggregate is
non-deterministic among ties and reports a spurious mismatch. Hashing rows and
sorting the hashes removes the need for a key at all.

\code{fold_empty = TRUE} compares the CSV as every reader actually sees it, which
is the behaviour \code{normalize_empty = TRUE} in \code{\link[=write_table_output]{write_table_output()}} targets.
}
