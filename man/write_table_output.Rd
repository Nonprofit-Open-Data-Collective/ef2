% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_write_table_output.R
\name{write_table_output}
\alias{write_table_output}
\title{Write a built table to CSV and/or Parquet from a single DuckDB temp table}
\usage{
write_table_output(
  db_tbl,
  table_name,
  year,
  con,
  output = c("csv", "parquet", "both"),
  dest = "CSV/",
  normalize_empty = TRUE,
  sort_key = "ORG_EIN",
  compression = "zstd",
  compression_level = 9L,
  row_group_size = 50000L,
  temp_name = "TEMP"
)
}
\arguments{
\item{db_tbl}{A lazy tibble / table reference to materialise.}

\item{table_name}{Character table id, e.g. \code{"F9-P00-T00-HEADER"}.}

\item{year}{Integer year.}

\item{con}{DBI connection to DuckDB.}

\item{output}{One of \code{"csv"}, \code{"parquet"}, \code{"both"}.}

\item{dest}{Destination directory or \verb{s3://} prefix. Defaults to \code{"CSV/"},
the path the package has always used.}

\item{normalize_empty}{Logical; collapse \code{''} to NULL in Parquet so it agrees
with the CSV on read-back. See Details.}

\item{sort_key}{Column to sort by before writing Parquet, or NULL. Sorting
makes row-group min/max statistics selective and shrinks the file: TY2023
header went from 61.5 MB to 56.2 MB sorted on \code{ORG_EIN}. Every published
table carries the KEYS columns, so \code{ORG_EIN} is always present.}

\item{compression}{Parquet codec. \code{"zstd"} gives 61.5 MB against snappy's
109.7 MB on the TY2023 header table.}

\item{compression_level}{Integer zstd level.}

\item{row_group_size}{Rows per Parquet row group. 50,000 costs 2\% in size
over the 122,880 default but cuts the rows scanned for a point lookup from
122,880 to 51,200.}

\item{temp_name}{Name of the DuckDB temporary table to materialise into.}
}
\value{
Invisibly, a character vector of the paths written.
}
\description{
Materialises \code{db_tbl} into a DuckDB temporary table once, then \code{COPY}s it to
CSV, Parquet, or both. Writing both from the same materialised relation is
what makes the two outputs consistent: they are two serialisations of one
relation, not a text file plus a re-parse of that text file.
}
\details{
\strong{Why Parquet from the temp table and not from the CSV.}
Everything in FLATXML is stored as VARCHAR -- \verb{R/04_write_to_duckdb.R} calls
\code{lapply(df, as.character)} before writing -- so the relation reaching this
function is already all-character. Copying it straight to Parquet involves no
type inference at any point. Converting the published CSV instead means
re-parsing text, and \code{data.table::fread()} and \code{read.csv()} both silently
cast \code{ORG_EIN} to integer: 7,969 of 20,000 sampled TY2023 header rows lose a
leading zero that way, e.g. \code{061721946} becomes \code{61721946}.

\strong{The empty-string problem, and why \code{normalize_empty} defaults to TRUE.}
The relation genuinely holds two different blanks. \code{pivot_wider(values_fill = "")} produces empty strings; the \code{right_join()} against KEYS produces SQL
NULLs. CSV writes these differently -- \code{""} against a bare empty field -- but
no reader distinguishes them on the way back in. DuckDB, \code{readr} and
\code{read.csv} all return NA for both. Parquet has a real null indicator and so
preserves the distinction, which means a faithful Parquet file would \emph{not}
agree with its own CSV sibling. In the TY2023 header table that is roughly
12.2 million cells, about 29\% of the file.

\code{normalize_empty = TRUE} collapses \code{''} to NULL in the Parquet output only,
reproducing what every CSV reader already does, so the two published formats
answer identically. The CSV output stays byte-identical to what the package
writes today. Set \code{normalize_empty = FALSE} to keep the distinction, but only
alongside a documented note that the two formats differ by design.
}
