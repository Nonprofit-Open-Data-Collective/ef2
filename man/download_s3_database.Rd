% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_update_db.R
\name{download_s3_database}
\alias{download_s3_database}
\title{Download an S3-hosted DuckDB archive to a local file}
\usage{
download_s3_database(
  year,
  version = "efile_v2_1",
  dest = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{year}{Tax year (integer or character).}

\item{version}{S3 version subfolder under duckdb/ (default "efile_v2_1").
Set NULL or "" for the unversioned path.}

\item{dest}{Destination path. Defaults to \verb{EFILE<year>.duckdb} in the
working directory.}

\item{overwrite}{Logical; if FALSE (default) and \code{dest} already exists, the
download is skipped.}
}
\value{
Invisibly, the local destination path.
}
\description{
Performs a plain (sequential/multipart) file download of the archived
\verb{EFILE<year>.duckdb} rather than streaming the whole database through
\code{httpfs} SQL. For large archives (tens of GB) this is dramatically faster
than a \verb{CREATE TABLE AS SELECT *} copy, and produces a local file that new
filings can be appended to directly (see \code{\link[=append_to_database]{append_to_database()}}).
}
\details{
Uses the AWS CLI (\verb{aws s3 cp --no-sign-request}) when available for
multipart parallelism, otherwise falls back to \code{\link[utils:download.file]{utils::download.file()}}.
}
