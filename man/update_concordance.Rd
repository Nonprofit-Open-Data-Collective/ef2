% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_concordance.R
\name{update_concordance}
\alias{update_concordance}
\title{Refresh the packaged concordance from GitHub}
\usage{
update_concordance(raw_copy = TRUE)
}
\arguments{
\item{raw_copy}{Logical; if TRUE (default) also refresh the raw CSV at
\code{inst/extdata/concordance.csv}.}
}
\value{
Invisibly, the refreshed concordance as a \code{data.table}.
}
\description{
Package-maintenance helper that replaces the packaged \code{concordance} dataset
(\code{data/concordance.rda}) with the most up-to-date master concordance file
from GitHub.
}
\details{
Downloads the master concordance CSV, normalizes column names to
lower case, optionally refreshes the raw copy at
\code{inst/extdata/concordance.csv}, and writes the compressed
\code{data/concordance.rda} via \code{\link[usethis:use_data]{usethis::use_data()}}. Run this from the
package source root during development, then rebuild/reinstall the package
to pick up the new data. Requires the \code{usethis} package.
}
\seealso{
\code{\link[=get_concordance]{get_concordance()}} to load the concordance at run time.
}
