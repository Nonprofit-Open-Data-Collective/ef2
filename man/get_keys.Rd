% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_utils_irs990efile.R
\name{get_keys}
\alias{get_keys}
\title{Get table keys for a single filing}
\usage{
get_keys(doc, url)
}
\arguments{
\item{doc}{An XML document object (from \code{xml2::read_xml()}).}

\item{url}{Character. Source URL of the filing (stored as \code{URL} and used to
derive the \code{OBJECTID}).}
}
\value{
A named list of key fields for the filing, suitable for coercion to a
one-row data frame:
\describe{
\item{EIN2}{Employer Identification Number formatted as \code{EIN-XX-XXXXXXX} (see \code{\link[=format_ein]{format_ein()}}).}
\item{OBJECTID}{Unique filing identifier prefixed with \verb{OID-}, derived from \code{url} (see \code{\link[=get_object_id]{get_object_id()}}).}
\item{ORG_EIN}{Raw Employer Identification Number digits from the return header.}
\item{ORG_EXEMPT_TYPE}{Tax-exempt status as a single string: \code{"501c2"} …
\code{"501c29"}, \code{"4947a1"}, \code{"527"}, or \code{NA}. Deliberately a string rather
than an integer, since 4947 and 527 are IRC sections and not 501(c)
subsections. Values match the \code{TaxStatus} naming in Giving Tuesday's
index. The 501(c) subsection is stored by the IRS as an XML \emph{attribute}
and reaches no published table except through this key; TY2009 has no
501(c)(3) checkbox and encodes \code{"3"} in that attribute instead, which
this variable harmonises. \code{"527"} is carried for completeness but has
never been observed. See \code{dev/UPSTREAM-ISSUES.md} EF2-12.}
\item{ORG_NAME_L1}{Filing organization name, line 1.}
\item{ORG_NAME_L2}{Filing organization name, line 2 (if present).}
\item{RETURN_AMENDED_X}{Logical; \code{TRUE} if the filing is an amended return
(read from the 990, 990-EZ or 990-PF amended-return element).}
\item{RETURN_GROUP_X}{Logical; \code{TRUE} if the filing is a group return for
affiliates. Always \code{FALSE} for the 990-EZ and 990-PF, which have no group returns.}
\item{RETURN_PARTIAL_X}{Logical; \code{TRUE} if the tax period spans fewer than 360 days (partial-year return).}
\item{RETURN_TAXPER_DAYS}{Number of days in the tax period (end minus begin, plus 1).}
\item{RETURN_TIME_STAMP}{Timestamp when the return was created/submitted.}
\item{RETURN_TYPE}{Return type (e.g. \code{"990"}, \code{"990EZ"}, \code{"990PF"}).}
\item{TAX_PERIOD_BEGIN_DATE}{Tax period begin date.}
\item{TAX_PERIOD_END_DATE}{Tax period end date.}
\item{TAX_YEAR}{Tax year covered by the filing.}
\item{URL}{Source URL of the raw XML filing.}
\item{VERSION}{IRS schema version of the return (\code{returnVersion} attribute).}
}
}
\description{
Collects the identifying metadata fields (OBJECTID, EIN, organization name,
tax period, return type/flags, etc.) that key every relational table built
from an IRS 990 e-file document. These are the "RDB keys" attached to every
one-to-one and one-to-many table so that unique tax filings can be
identified, filtered, and linked across tables.
}
\examples{
\dontrun{
doc <- xml2::read_xml(url)
xml2::xml_ns_strip(doc)
get_keys(doc, url)
}
}
\seealso{
\code{\link[=add_keys]{add_keys()}}, which attaches these keys to relational tables.
}
