% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_update_db.R
\name{append_to_database}
\alias{append_to_database}
\title{Append a temporary DuckDB (new filings) into an existing local DuckDB}
\usage{
append_to_database(
  target_db,
  temp_db_path,
  tables = base::c("KEYS", "FLATXML", "ATTRIBUTES")
)
}
\arguments{
\item{target_db}{Path to the base DuckDB to append into (modified in place).}

\item{temp_db_path}{Path to the temporary DuckDB with the new filings.}

\item{tables}{Character vector of tables to append
(default \code{c("KEYS", "FLATXML", "ATTRIBUTES")}).}
}
\value{
Invisibly, \code{target_db}.
}
\description{
Inserts the rows from a temporary "update" database (built by
\code{\link[=build_database]{build_database()}} with \code{is_update = TRUE}) into a base database in place,
aligning schemas. This is the local counterpart of \code{\link[=merge_databases]{merge_databases()}}:
pair it with \code{\link[=download_s3_database]{download_s3_database()}} to update a large archive without
streaming the whole thing through \code{httpfs}.
}
