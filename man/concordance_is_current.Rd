% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_concordance.R
\name{concordance_is_current}
\alias{concordance_is_current}
\title{Check whether the packaged concordance is current}
\usage{
concordance_is_current(verbose = TRUE)
}
\arguments{
\item{verbose}{Logical; if TRUE (default) messages a short summary.}
}
\value{
Logical \code{TRUE} if the packaged data matches GitHub, \code{FALSE} if it is
out of date, or \code{NA} (with a warning) if the GitHub version could not be
retrieved (e.g. offline).
}
\description{
Compares the concordance shipped with the package (\code{data(concordance)}) to
the current master concordance file on GitHub, so a stale packaged copy can
be detected (e.g. at build/check time).
}
\seealso{
\code{\link[=update_concordance]{update_concordance()}} to refresh the packaged data.
}
